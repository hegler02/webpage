# 미리내의 별자리 — 승인 시안 3 구현 검증

final result: passed

## 기준과 증거

- 승인 기준: 2026-09-15 사용자가 선택한 세 번째 생성 이미지 `exec-001ac2ee-b96a-4c2a-ba3f-afbd19dbec8f.png` (1586 × 992). 대화에 보존된 원본을 열어 실제 화면과 같은 비교 입력에 함께 놓고 검토했다.
- PC 전체 비교: `docs/constellation-qa/desktop-comparison.jpg` (1348 × 2578). 브라우저가 렌더링한 1176 × 888 CSS px 프레임을 포함한다.
- 모바일 수정 후 증거: `docs/constellation-qa/mobile-final.jpg` (1348 × 926). 326 × 888, 390 × 844, 360 × 844 CSS px 프레임을 포함한다.
- 브라우저 캡처는 1 CSS px 기준으로 비교했다. 바깥 검증 캔버스와 브라우저 스크롤바를 제품 영역에서 제외했다. 원본 시안은 PC와 모바일을 한 이미지에 배치한 목업이므로 각 콘텐츠 영역을 기준으로 비교했다.
- 상태: 가을에서 시작 / 지도 / 자막에서 배운 것 선택. PC 오른쪽, 모바일 아래쪽에 읽기 패널이 열린 상태.
- 집중 비교: 모바일의 노드 제목, 카드 폭, 이미지, 선택 표시와 하단 읽기 패널을 별도 최종 캡처로 확인했다.

## 발견 → 수정 → 재확인

1. [P1, 해결] 모바일에서 숨김 처리한 `각자의 시간` 카드가 다시 표시됐다. 더 구체적인 개념 카드의 flex 규칙이 숨김 규칙을 덮었다. `.node.mobile-hidden`에 명시적인 숨김 우선순위를 적용했다. 최종 캡처에서 세 개의 주요 기록만 나타난다.
2. [P2, 해결] 좁은 모바일에서 선택한 판단 카드 제목의 마지막 글자가 고립됐다. 선택 카드가 두 열을 활용하고 오른쪽에 정렬되도록 폭을 조정했다. 최종 326/360/390 화면에서 제목이 정상적으로 읽힌다.
3. [P2, 해결] 읽기 패널의 한국어 단어가 중간에서 잘렸다. 본문에 `word-break: keep-all`을 적용하고 긴 문자열은 overflow-wrap으로 보호했다. 수정 후 PC 및 모바일 캡처에서 확인했다.
4. 이전 자동 검토 서비스의 용량 오류로 브라우저와 원격 저장이 중단됐던 사실을 보존한다. 이번 재시도에서 바이너리 저장이 성공했다. 로컬 미리보기는 현재 Work Mode의 `sites-preview` 실행 방식과 4173 포트를 사용해 정상 연결했다. 과거 중단을 화면 검증 성공으로 간주하지 않았다.

## 다섯 가지 디자인 확인

- 서체: 로컬 Nanum Gothic 400/700. 제목·작품명·본문의 위계, 줄간격과 줄바꿈을 확인했다.
- 배치: 검정 배경의 세 열과 오른쪽 아이보리 패널, 모바일의 세 기록과 하단 패널을 유지한다. 모든 검증 폭에서 scrollWidth와 clientWidth가 일치한다.
- 색: 검정/흰색/따뜻한 회색/아이보리 구성과 선택 대비를 유지한다.
- 이미지: 실제 아카이브 썸네일과 생성한 그림자 이미지를 사용했다. 깨진 이미지 0개. 작품은 본래 비율을 보존한다.
- 내용: 목업의 예시 문구를 실제 공개 에세이·제작 판단·서지 기록으로 교체했다. 근거 없는 인과관계 대신 출처와 연결 이유를 표시한다. 읽기 패널이 길어진 부분은 실제 본문을 제공하기 위한 의도적인 차이다.

## 동작 검증

- PC: 에세이 선택, 논문과 연구 19개(논문 16편 + 탐색 주제 3개), 여왕벌 논문 검색/선택, 검색 결과 없음, 전체 77개 기록, 초기화, 선택 URL 재접속, 읽기 패널 닫기.
- 모바일: 메뉴 열기/닫기, 검색 열기/닫기, 달팽이 검색/선택, 하단 읽기 패널, 초기화, 현재 연결 링크 복사 성공.
- 이미지가 모두 로드된 뒤 1176/326/390/360 CSS px에서 수평 넘침 0, 깨진 이미지 0 확인.
- 콘솔에 관측된 오류는 브라우저 확장 프로그램의 metadata 전송 오류였다. 앱 코드 오류는 관측되지 않았다.
- 기존 저장소 release gate, 5개 데이터 검사, JavaScript 구문 및 Git whitespace 검사를 통과했다.

## 다음 개선

- [P3] 실제 사용에서 연결 경로의 가독성을 관찰하고, 선택한 관계의 설명을 지도에서도 더 간결하게 보여 줄 수 있다.
- [의도적 차이] 모바일에도 지도/목록과 범위 선택을 제공한다. 목업에 없는 조작 공간만큼 첫 읽기 패널의 시작 위치가 내려간다. 기록이 쌓여도 탐색 기능을 유지하기 위한 선택이다.

공개 배포 성공 여부는 별도의 실제 배포 상태와 공개 URL로 확인한다. 이 문서의 passed는 화면·동작 검증 결과다.
