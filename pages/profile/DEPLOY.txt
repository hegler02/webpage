The repository root is the Vercel project root. Vercel runs `python3 tools/release_gate.py` before publishing and serves the static output from the repository root. Public profile routes are declared in `site.manifest.json`, rewritten by the root `vercel.json`, and generated archive outputs must stay synchronized with `data/message-bodies.json` through `python3 tools/render_archive.py`.
