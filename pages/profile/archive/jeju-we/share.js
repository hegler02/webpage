document.querySelector('#share-page').addEventListener('click',async()=>{const url=document.querySelector('link[rel="canonical"]').href;const status=document.querySelector('#share-status');try{if(navigator.share){await navigator.share({title:document.title,url});status.textContent='';}else{await navigator.clipboard.writeText(url);status.textContent='페이지 주소를 복사했습니다.';}}catch(error){if(error.name==='AbortError')return;status.replaceChildren(document.createTextNode('이 주소를 복사해 공유하세요: '));const input=document.createElement('input');input.readOnly=true;input.value=url;input.setAttribute('aria-label','페이지 공유 주소');status.append(input);input.select();}});
